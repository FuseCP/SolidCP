For this project to fully build you need to have a WSL distro with rpmbuild installed. If not, the Fedora rpm package won't be built.

For Fedora, the .specs file must reside in the Fedora\SPECS folder.